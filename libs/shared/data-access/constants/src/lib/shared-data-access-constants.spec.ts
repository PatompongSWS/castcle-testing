import { sharedDataAccessConstants } from './shared-data-access-constants';

describe('sharedDataAccessConstants', () => {
  it('should work', () => {
    expect(sharedDataAccessConstants()).toEqual('shared-data-access-constants');
  });
});
