import { sharedDataAccessInterfaceApi } from './shared-data-access-interface-api';

describe('sharedDataAccessInterfaceApi', () => {
  it('should work', () => {
    expect(sharedDataAccessInterfaceApi()).toEqual(
      'shared-data-access-interface-api'
    );
  });
});
